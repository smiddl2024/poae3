import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css"; // ← change to ../styles/globals.css if globals.css lives in /styles

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "Applied Engineering Hub",
  description: "EngageHub — project roadmap and lessons",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${inter.variable} bg-gray-100`}>{children}</body>
    </html>
  );
}
